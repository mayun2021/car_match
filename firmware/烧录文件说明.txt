优先方式：打开 keil\CarMatch_MSPM0G3507.uvprojx，连接 XDS110/CMSIS-DAP，
按 F7 编译，再按 F8 下载。

*** 注意：本目录固件已过期 ***
下面几个预编译文件是"跑完一圈"还靠四探头全黑停车线判定时编译的。
源码已改成纯偏航角判定（从0转过350°直接停车，不再需要看到停车线
标记）。烧录前请先在 Keil5 里按 F7 重新编译，不要直接烧这些旧文件。

本目录固件为稳态循迹 V2。烧录后 OLED 第一行应显示 V2。

预编译文件：
  CarMatch_MSPM0G3507.hex  通用 Intel HEX
  CarMatch_MSPM0G3507.axf  带调试信息的 Keil 可执行文件
  CarMatch_MSPM0G3507_STABLE_V2.hex  相同固件的 V2 文件名副本
  CarMatch_MSPM0G3507_STABLE_V2.axf  相同固件的 V2 文件名副本

目标芯片：MSPM0G3507SPTR，LQFP-48，128 KB Flash。
编译结果：0 Error(s), 0 Warning(s)。

操作按键：A30（K3）启动/停止；A27 切换模式；A17 校准。
启动条件：模式1下先调整到 MASK=6；显示 ALIGN V2 时电机不会启动。
丢线保护：只低速缓弯，不允许原地反转；持续丢线 350 ms 自动停车。
首次烧录后必须架空车轮检查电机方向和停机逻辑，再落地低速调试。
