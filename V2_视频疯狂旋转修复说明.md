# V2：视频中“疯狂原地旋转”的原因与修复

## 视频结论

逐帧检查 `6087868db6ff4cc185bdf396fbf5b4a7.mp4` 后可以看到：按下启动时，
车头前伸的四路红外探头并没有压在黑线上。旧程序在 `MASK=0` 时立即执行：

```text
左轮 +220，右轮 -220
```

这就是视频中车辆始终围绕轮轴中心旋转的直接原因，并非电机突然损坏。

旧巡线参数还有第二个风险：四路数字探头的误差每次会跳变约 333，而旧
`KD=0.18` 按 5 ms 周期计算时会产生很大的微分冲击，输出会反复达到限幅，
使一只轮再次反转。

## V2 修改

- 模式1/3只有在探头已经接近黑线中心时才接受 A30 启动。
- OLED 显示 `ALIGN V2` 时拒绝启动；调整到 `MASK:0110` 后再按 A30。
- 基础输出从 32% 降为 22%。
- 使用适合四路数字探头的低速纯 P 控制。
- 转向修正限制为 14%，所有正常循迹命令都在 `+80` 到 `+360`，不反转车轮。
- 丢线时两轮仍同向，只以 `180/80` 或 `80/180` 缓弯。
- 连续丢线 350 ms 自动停止，禁止无限找线。
- 四路输入使用三次多数表决，削弱电位器临界点和电机干扰造成的跳变。
- 启动 700 ms 内偏航超过 35° 自动停车，用于防止电机方向或接线异常。

## 正确上车方式

1. 车轮先架空，确认 OLED 第一行显示 `V2`。
2. 模式保持 `M1`。
3. 把车身方向沿黑线切线摆正，不只是让底盘压住线。
4. 确认车头最前方探头位于黑线中央，OLED 显示 `MASK:0110`。
5. 放下车辆后短按 A30/K3。

如果在 `MASK=6` 一直稳定不变的情况下，车辆仍然原地转动，才说明某一侧电机
物理方向相反。此时先架空双轮拍一段正面视频，再决定只切换
`ROBOT_RIGHT_MOTOR_REVERSE`，不要同时改传感器方向和两个电机方向。
